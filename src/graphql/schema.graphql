type User {
  id: ID!
  name: String!
  email: String!
}

type Query {
  getUsers: [User]!
}

input CreateUserInput {
  name: String!
  email: String!
}

type Mutation {
  createUser(input: CreateUserInput!): User!
}
